export default class Vehicule {
    speed;
    constructor(model, traccion, min, max){
        this.model = model;
        this.traccion = traccion;
        this.min = min;
        this.max = max;
    }

}